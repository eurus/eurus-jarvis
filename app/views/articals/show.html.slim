.row
  .col-md-12.text-center
    = link_to '修改', edit_artical_path(@artical),class: "btn btn-primary pull-right"
    = notice_tag notice
    h3
      = @artical.title
    h5
      = "编辑：#{User.find(@artical.user_id).try :nickname}"
    h6
      a href="#{@artical.origin}" 来源
    article
      = @artical.content

